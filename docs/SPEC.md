# CIRCLE BATTLE 仕様書

TikTok LIVE 連動バトルゲーム。最終更新: 2026-09-07

---

## 1. 概要

### 1.1 ゲームの構造

```
        【自動生成される敵】
                VS
        【TikTok LIVE 視聴者】
```

視聴者が TikTok LIVE 上で **LIKE / FOLLOW / SHARE / GIFT / 入室** を行うと、
その視聴者専用の円が生まれ、以後の行動でレベルが上がっていく。
円は自動生成される敵を攻撃し、撃破数とスコアでリアルタイムランキングを競う。

### 1.2 設計方針

| 方針 | 内容 |
| --- | --- |
| チーム概念を持たない | `Team A` / `Team B` のような固定の陣営は実装しない。ランキングは常に個人単位 |
| 画面が止まらない | TikTok イベントが 0 件でも、敵の生成・移動・衝突・撃破のループは回り続ける |
| 視聴者に不利益を出さない | アイテムも敵の強化も、既存の視聴者が弱くなる方向には働かない |
| TikTok を game 層に持ち込まない | TikTok 固有の語彙は Event Router までで止める |
| 数値はすべて設定に出す | ゲームロジックに数値を直書きしない。調整は `js/config.js` の 1 ファイルで完結する |

### 1.3 想定環境

| 項目 | 内容 |
| --- | --- |
| 実行環境 | ブラウザ（Chromium 系推奨）。`file://` でも動作 |
| 配信 | OBS ブラウザソース 1080×1920（縦） |
| 依存 | なし（外部ライブラリ・ビルド不要）。中継サーバーのみ Node.js 20.12+ |
| 音源・画像 | ファイル不要（効果音は WebAudio 合成、アイコンは TikTok から取得） |

---

## 2. システム構成

### 2.1 リポジトリ

| リポジトリ | 役割 |
| --- | --- |
| `circlebattle` | ゲーム本体（ブラウザで動く） |
| `tikhub` | TikTok LIVE への接続、イベント正規化、SSE 中継、ゲーム配信 |
| `kawaiivsbeautiful` | 既存の別ゲーム。本ゲームとは独立（接続部分の実装を流用） |

### 2.2 データの流れ

```
TikTok LIVE
    │  (tiktok-live-connector / 非公式)
    ▼
tikhub  src/sources/tiktok.js     受信
        src/events.js             正規化（profileImageUrl の抽出を含む）
        src/bridge.js             SSE 配信 + ゲーム本体の配信
    │  Server-Sent Events  http://127.0.0.1:8787/events
    ▼
circlebattle
    js/tiktok-adapter.js   受信口（SSE / WebSocket）
    js/event-router.js     TikTok の語彙 → ゲームイベント     ★ここが境界
    js/game-session.js     ゲームイベント → レベル・円・得点配分
    js/game.js             敵・円・アイテム・物理・戦闘
    js/director.js         ウェーブと特殊イベント（敵だけを動かす）
    js/weapons.js          武器の描き分け（当たり判定と能力は game.js）
    js/leaderboard.js      ユーザー成績とランキング
    js/app.js              上を 1 つに束ねた「ゲームの状態」
    js/game-view.js        描画・音・演出（状態を読むだけ）
    js/renderer.js         描画（canvas + DOM）
```

### 2.2.1 ウィンドウ構成

操作画面（`index.html`）とゲーム画面（`game.html`）に分かれる。
**ゲームは 1 つだけ**で、ゲーム画面は別プロセスではない。

```
        操作画面 index.html
        ├ TikTok LIVE の接続        ┐
        ├ 音量 / BGM(MP3)           │ 触るものは全部こちら
        ├ ゲーム・敵・イベント設定    │
        ├ テストパネル               ┘
        └ CB.app  ← ゲームの状態はここに 1 つだけ
                 ▲
                 │ window.opener.CB.app（同じオブジェクトを読む）
                 │
        ゲーム画面 game.html
        ├ Game Status
        ├ Battle Field（正方形）
        ├ TOP 10 PLAYER
        └ LIVE EVENT（数行）
```

| | 操作画面 | ゲーム画面 |
| --- | --- | --- |
| 用途 | 配信者が触る | 配信に映す（OBS でキャプチャ） |
| 画面比 | PC 向け・可変 | 9:16（1080×1920 基準） |
| 音 | 鳴らさない | 効果音・BGM |
| ゲームの状態 | **持つ** | 操作画面のものを読む |
| 単独で開いた場合 | – | 自分で組み立てて動く（OBS のブラウザソース用） |

設定は `app.settings`（効果音・BGM・背景）と `config`（ゲームの数値）で共有され、
操作画面で変えた瞬間にゲーム画面へ反映される。

#### 繋ぎ直し

操作画面を読み込み直すと、あちらの `CB.app` は**作り直される**。ゲーム画面が
古いほうを掴んだままだと、2 つの画面で別々のゲームが動き、配信には
そちらが映ってしまう。

ゲーム画面は 0.8 秒ごとに `CB.findSharedApp()` を見て、掴んでいるものと
違えば繋ぎ直す（`RECONNECTED TO CONTROL WINDOW`）。操作画面が閉じられた
ときは描画を止めて `CONTROL WINDOW CLOSED` を出す（黙って固まらせない）。

`game.html` を URL で直接開いた場合は見張らない（共有する相手がいないため）。

時刻は **`Date.now()` に統一**している。`performance.now()` はウィンドウごとに
原点が違うため、2 つの画面から同じゲームを進めると時間が飛ぶ。
`app.update()` は直前に進めたばかりなら何もしないので、両方の画面が毎フレーム
呼んでも二重には進まない。どちらが進めるかを決め打ちにしないのは、
決めた側のウィンドウを最小化すると `requestAnimationFrame` が止まり、
ゲームごと止まってしまうため。

**Event Router から下流には TikTok 固有の情報が一切流れない。**
`game.js` / `game-session.js` / `leaderboard.js` に `giftId`・`diamondCount`・`uniqueId` は出現しない。

### 2.3 ファイル構成

| ファイル | 役割 | TikTok を知るか | Node からテスト可能か |
| --- | --- | --- | --- |
| `js/config.js` | 全設定値 | – | ○ |
| `js/tiktok-adapter.js` | 中継サーバーからの受信 | **はい** | ○（URL 解決のみ） |
| `js/event-router.js` | イベント翻訳 | **はい（最後）** | ○ |
| `js/game-session.js` | レベル制御・得点配分 | いいえ | ○ |
| `js/game.js` | バトルエンジン | いいえ | ○ |
| `js/leaderboard.js` | ランキング | いいえ | ○ |
| `js/demo.js` | 無人時のデモ視聴者 | いいえ | ○ |
| `js/avatars.js` | アイコンのキャッシュ | いいえ | – |
| `js/audio.js` | 効果音 | いいえ | ○（間引き判定のみ） |
| `js/director.js` | ウェーブと特殊イベント | いいえ | ○ |
| `js/weapons.js` | 武器の描き分け | いいえ | ○（段の計算のみ） |
| `js/renderer.js` | 描画 | いいえ | – |
| `js/app.js` | ゲーム一式の組み立てと共有設定 | いいえ | – |
| `js/game-view.js` | 描画・音・演出の配線 | いいえ | – |
| `js/controls.js` | テストパネル | いいえ | – |
| `js/control.js` | 操作画面の設定 UI | いいえ | – |
| `js/main.js` | 操作画面の起動 | いいえ | – |
| `js/game-main.js` | ゲーム画面の起動 | いいえ | – |

| ページ | 役割 |
| --- | --- |
| `index.html` + `css/control.css` | 操作画面 |
| `game.html` + `css/game.css` | ゲーム画面（9:16） |

---

## 3. イベント仕様

### 3.1 受信する TikTok イベント

| TikTok | ゲームイベント | 備考 |
| --- | --- | --- |
| `like` | `LIKE` | `count` を使用（まとめて届く） |
| `follow` | `FOLLOW` | |
| `share` | `SHARE` | |
| `gift` | `GIFT` | 連打中（`finished === false`）は無視し、確定時のみ |
| `member` / `join` / `enter` | `JOIN` | 入室 |
| `chat` / `comment` | `COMMENT` | **ゲームには影響しない**（将来のコマンド用） |
| `viewer` ほか | – | 破棄 |

### 3.2 ユーザー情報の正規化

| フィールド | 取得元（優先順） |
| --- | --- |
| `id` | `user.id` → `uniqueId`（userId が取れない配信への対応） |
| `uniqueId` | `user.uniqueId` → `displayId` → `username` |
| `displayName` | `user.nickname` → `displayName` |
| `profileImageUrl` | `profileImageUrl` → `profilePictureUrl` → `avatarThumb.urlList[]` → `avatarMedium` → `avatarLarge` |

プロフィール画像は 100x100 の webp を優先。取得できない場合は `null`（画面側で頭文字アイコン）。

### 3.3 ギフトのコイン換算

```
優先順位:  gifts.byId[giftId]  →  gifts.byName[giftName]  →  diamondCount × coinsPerDiamond  →  defaultCoins
コイン価値 = 単価 × repeatCount
```

| 設定 | 既定値 |
| --- | --- |
| `gifts.coinsPerDiamond` | 1 |
| `gifts.defaultCoins` | 1 |
| `gifts.byId` / `byName` | 空（個別指定用） |

---

## 4. レベル仕様

### 4.1 レベルの上がり方

**円は増えるのではなく育つ。** 1 人が育てている円は 1 つで、行動するとその円のレベルが上がる。

| 操作 | 上がるレベル | 設定 |
| --- | --- | --- |
| いいね 10 回 | +1 | `levels.likesPerLevel: 10` |
| フォロー | +10 | `levels.follow: 10` |
| シェア | +5 | `levels.share: 5` |
| ギフト | コイン 1 につき +1（最低 +1） | `levels.giftLevelsPerCoin: 1` |
| 入室 | レベル 20 の円を生成（1 人 1 回のみ） | `levels.join: 20` / `joinOncePerUser: true` |

- レベル範囲は **1 〜 100**（`levels.max: 100`）
- いいねの端数は**ユーザーごとに**繰り越す（7 + 5 = 12 → 1 レベル、余り 2）
- 入室ボーナスは 1 人 1 回のみ。入り直しても再取得できない

### 4.2 円の能力値

```
値 = base × レベル ^ exp
```

| 値 | base（Lv1） | 指数 | 上限 |
| --- | --- | --- | --- |
| HP | 300 | 0.65 | – |
| 攻撃力 | 36 | 0.6 | – |
| 半径 | 20 | 0.19 | 48 |
| 速さ | 165 | 0.22 | 700 |
| 攻撃間隔 | 300ms | – | – |

| レベル | HP | 攻撃力 | 半径 | 速さ |
| --- | --- | --- | --- | --- |
| 1 | 300 | 36 | 20.0 | 165 |
| 10 | 1,340 | 143 | 31.0 | 274 |
| 20 | 2,103 | 217 | 35.3 | 319 |
| 50 | 3,815 | 376 | 42.1 | 390 |
| 80 | 5,178 | 499 | 46.0 | 433 |
| 100 | 5,986 | 571 | 48.0 | 454 |

レベルアップ時、増えた HP 分はそのまま回復する（育てて打たれ弱くならない）。

### 4.3 最大レベル（MAX 状態）

レベル 100 到達で **20 秒間の MAX 状態**に入る。

| 項目 | 内容 |
| --- | --- |
| 継続時間 | 20 秒（`levels.maxDurationMs`、0 で無期限） |
| 攻撃力 | ×2.5（`levels.maxBonus.attack`） |
| 速さ | ×1.8（上限 700） |
| HP | 突入時に全快 |
| 表示 | 虹色オーラ + 金色の `MAX` バッジ + 残り時間の白いリング |
| 終了時 | 燃え尽きて消滅（`circle:removed` reason `burnout`） |

MAX 状態の実効値: **攻撃力 1,428 / 速さ 700**

高額ギフトやアイテムで一気に 100 に達した場合も同様に MAX 状態へ入る。

### 4.4 MAX 後の再スタート

MAX の円が残っている状態で次の行動があった場合、**新しい円**が生まれる。
新しい円の初期レベルは**その行動ぶんのレベル**（`levels.restartAtActionLevel: true`）。

| 行動 | 新しい円の初期レベル |
| --- | --- |
| 10 いいね | 1 |
| フォロー | 10 |
| 50 コインのギフト | 50 |

円が敵に倒された場合も、次の行動で新しい円が生まれる。

### 4.5 保有数と順番待ち（キュー）

| 設定 | 既定値 | 内容 |
| --- | --- | --- |
| `limits.maxPerUser` | 5 | 1 人がフィールドに置ける円の上限 |
| `limits.queue` | 5 | 順番待ちの列の長さ |
| `limits.maxCircles` | 400 | フィールド全体の上限（安全弁） |

```
フィールド [Lv100][Lv100][Lv100][Lv100][Lv100]   ← 5 個で満員
順番待ち                          [Lv30][Lv12]   ← 次に入る円
```

- 上限に達した状態で新しい円ができる場合、**押し出さずに列へ積む**
- 円が 1 つ減る（燃え尽き / 撃破）と、**列の先頭が自動で投入される**
- 列も満杯の場合、**列の最後の円のレベルに加算**する（送ったぶんは失われない）
- 全体上限に達した場合のみ、最古の円から押し出す（この場合は列から補充しない）
- 順番待ちの数はランキングに `Lv100 +2` の形で表示

---

## 5. 敵仕様

### 5.1 種類

| id | 表示 | HP | 半径 | 速さ | 攻撃力 | 撃破ポイント | 出現比 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `normal` | NORMAL | 100 | 20 | 58 | 6 | 1 | 62 |
| `strong` | STRONG | 300 | 28 | 48 | 12 | 5 | 24 |
| `elite` | ELITE | 1,000 | 40 | 38 | 26 | 20 | 11 |
| `boss` | BOSS | 5,000 | 66 | 26 | 60 | 100 | 3 |
| `rare` | RARE | 2,500 | 34 | 120 | 20 | 300 | 0 |

攻撃間隔は 600〜700ms。種類は `enemies.types` に 1 行足すだけで追加でき、
実行中も `engine.addEnemyType()` で追加できる。

`rare` は出現比 0。通常は湧かず、特殊イベント（§5.4）でだけ出る。
速く（120）小さく（34）逃げ回るので、倒せれば大きい（300）。

### 5.2 生成

| 設定 | 既定値 | 内容 |
| --- | --- | --- |
| `spawn.intervalMs` | 1000 | 生成間隔 |
| `spawn.initialCount` | 8 | 起動時の配置数 |
| `spawn.minAlive` | 6 | 下限。割ったら間隔を待たずに補充 |
| `spawn.maxAlive` | 48 | 上限 |
| `spawn.circlesPerExtraEnemy` | 4 | 視聴者円 4 個ごとに下限 +1 |
| `spawn.edgeInset` | 0.13 | 壁からどれだけ内側に湧かせるか |

- 湧く位置は壁の上ではなく内側（壁沿いに張り付く敵を作らないため）
- 辺に沿う位置も端 10% を避ける（角に固まらないため）
- 他の敵と重なる位置しか無い場合は**生成を見送る**（次の間隔で再試行）

### 5.3 場の勢力に応じた強化

```
HP 倍率 = 1 + (フィールド上の視聴者円の合計レベル ÷ 900)   上限 15 倍
```

- 上げるのは **HP のみ**。撃破ポイントも敵の攻撃力も変えない
  （育っていない視聴者が不利にならないため）
- これが無い場合、育った円が湧いた瞬間の敵を溶かし、毎秒 200 体以上が撃破されて
  画面から敵が消える（100 人負荷で実測）

### 5.4 ウェーブと特殊イベント

`js/director.js`。**敵しか触らない。**視聴者の円もランキングも消さない。
区切りのたびに積み上げが消えると、育てる意味がなくなるため。

| 設定 | 既定値 | 内容 |
| --- | --- | --- |
| `director.waveMs` | 90,000 | ウェーブが進む間隔 |
| `director.eventChance` | 0.5 | ウェーブ更新時に特殊イベントが起きる確率 |

起きうるイベント（`director.events` の `weight` で抽選）:

| id | 表示 | 内容 | 比 |
| --- | --- | --- | --- |
| `boss` | BOSS APPEARED | BOSS ×2 | 3 |
| `swarm` | SWARM INCOMING | ランダムな敵 ×12 | 3 |
| `rare` | RARE ENEMY | RARE ×1 | 2 |
| `elite` | ELITE SQUAD | ELITE ×5 | 2 |

- 平均すると **3 分に 1 回**程度。毎回起きると「特殊」ではなくなる
- `director.trigger('boss')` で手動でも起こせる（テストパネルの SWARM ボタン）
- 出す文字は `label` / `sub`。ゲームの進行は止めない（バナーが 2.2 秒出るだけ）

---

## 5.5 武器の進化

武器は円のまわりを**回りながら、当たった敵を斬る**。段が上がるほど形が変わり、
届く距離・当たる腕の数・特殊能力が増える。

役割は 2 つに分かれている。

| | 場所 | 何を持つか |
| --- | --- | --- |
| ルール | `js/game.js` | 回転の角度・当たり判定・特殊能力 |
| 見た目 | `js/weapons.js` | 段ごとの形と描き分け |

**角度は円が 1 つだけ持つ**（`circle.weaponAngle`）。描画はそれを読むだけなので、
「当たったように見えたのに当たらない」が起きない。段の引き当ても
`weaponTierFor()` 1 つに集約してある（テストで両者の一致を固定: §15）。

円そのものの HP・攻撃力・速さ・半径は今までどおり `viewers.scaling`（§4.2）が
決める。武器が足すのは**武器のぶんの攻撃と、段ごとの特殊能力**だけ。
円の基本値を武器側で書き換えないのは、どちらが効いているのか分からなく
なるのを避けるため。

### 5.5.1 段

Lv1〜100 を 10 段に分ける。段が変わると**形**が変わる。

| Lv | id | 名前 | 見た目 | 腕 | 届く距離 | 威力 | 回転 | 特殊能力 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | `none` | NO WEAPON | 武器なし | – | – | – | – | – |
| 10 | `blade` | NEON BLADE | 小型の光る剣 1 本 | 1 | 1.62 | ×1.0 | 2.6 | – |
| 20 | `twin` | TWIN BLADES | 光る剣 2 本 | 2 | 1.66 | ×1.0 | 3.0 | TWIN STRIKE |
| 30 | `spear` | ENERGY SPEAR | 長いエネルギー槍 | 1 | **1.82** | ×1.45 | 2.4 | PIERCE |
| 40 | `axe` | PLASMA AXE | 大型の斧 | 1 | 1.78 | ×1.6 | 2.1 | CLEAVE |
| 50 | `scythe` | ENERGY SCYTHE | 大型の鎌が周囲を回る | 1 | 1.7 | ×1.5 | 3.4 | LIFESTEAL |
| 60 | `chakram` | TWIN CHAKRAMS | 2 つの円刃が旋回 | 2 | 1.72 | ×1.4 | **4.4** | WHIRL |
| 70 | `cannon` | PLASMA CANNON | 円の横に大型砲 2 門 | 2 | 1.7 | ×1.5 | 1.8 | BARRAGE |
| 80 | `wings` | ENERGY WINGS | 剣 + 背後のエネルギー翼 | 3 | 1.76 | ×1.5 | 2.2 | AEGIS |
| 90 | `orbital` | ORBITAL WEAPON | 5 つのオーブが周回 | **5** | 1.72 | ×1.4 | 3.6 | ORBIT STRIKE |
| 100 | `core` | LEGENDARY CORE | コアの欠片 + 周回する輪 | 3 | 1.75 | ×1.6 | 4.0 | NOVA |

届く距離は円の半径の倍率、回転はラジアン/秒。
段を足すには `weapons.tiers` に 1 行と、`js/weapons.js` の `PAINTERS` に
同じ id の描き方を足す。

### 5.5.1.1 当たり判定

判定は 2 つだけ。**敵との距離は戦闘ループが既に出している**ので、
足すのは角度の比較 1 回で、円が 150 個あっても重くならない。

```
距離   円の半径 × innerReach   〜   円の半径 × reach + 敵の半径
角度   いちばん近い腕との差   ≤   arc + 敵の見込む角
```

- 腕は等間隔（`arms` 本）。6 本でも角度の比較は 1 回で済む
- `offset` は「絵で腕が生えている向き」。PLASMA CANNON は円の真横に砲を
  出すので `π/2`、ENERGY SCYTHE は刃が片側に伸びるので `0.5`
- 近い敵ほど当たり判定の角度を広げる（同じ大きさでも見込む角が大きくなるため）
- 武器の攻撃は円本体の殴り合いとは**別に**数える（`combat.intervalMs`、既定 300ms）
- 当たった敵が 1 体もいなければ間隔は減らない（空振りで待たされない）

| 設定 | 既定値 | 内容 |
| --- | --- | --- |
| `weapons.combat.intervalMs` | 300 | 武器で殴る間隔 |
| `weapons.combat.damage` | 0.5 | 円の攻撃力に対する武器の倍率（段の倍率がさらに掛かる） |
| `weapons.combat.innerReach` | 1.0 | 当たり判定の内側 |

### 5.5.1.2 特殊能力

段ごとに専用の仕組みを書くと 10 個の別々のゲームになるので、
**5 つの部品の組み合わせ**で作る。強さの調整も 1 か所で済む。

| 部品 | 内容 |
| --- | --- |
| `maxTargets` | 一振りで当たる敵の数 |
| `lifesteal` | 武器で与えたダメージのうち、自分の HP に戻る割合 |
| `rate` | 武器の攻撃間隔の倍率（小さいほど速い） |
| `damageTaken` | 受けるダメージの倍率（小さいほど固い） |
| `nova` | `{ intervalMs, damage }` 射程内の敵**全部**への衝撃波 |

| Lv | 能力 | 中身 |
| --- | --- | --- |
| 20 | TWIN STRIKE | 一振りで 2 体 |
| 30 | PIERCE | 一振りで 2 体 + 届く距離が一番長い |
| 40 | CLEAVE | 一振りで 3 体 |
| 50 | LIFESTEAL | 一振りで 3 体 + 与ダメージの 35% を回復 |
| 60 | WHIRL | 一振りで 4 体 + 回転が最速 |
| 70 | BARRAGE | 一振りで 3 体 + 攻撃間隔 0.55 倍 |
| 80 | AEGIS | 一振りで 4 体 + 受けるダメージ 0.6 倍 |
| 90 | ORBIT STRIKE | 一振りで 5 体（腕も 5 本）+ 受けるダメージ 0.8 倍 |
| 100 | NOVA | 上の全部 + 2.4 秒ごとに射程内の敵全部へ攻撃力 ×3.5 |

NOVA の 1 発目は**最大レベルに届いた瞬間**に出る（区切りの演出を兼ねる）。

### 5.5.2 中間レベル

段の中でも 1 レベルごとに育つ。**見た目だけ**が変わり、当たり判定と
特殊能力は段ごとに一定。段ごとの絵は 1 つだけで、**貼り方**を変えて
成長を表す（レベル 100 種類ぶんの絵は持たない）。

| 何が変わるか | どう変わるか |
| --- | --- |
| 大きさ | Lv1 で `minScale` (0.8)、Lv100 で `maxScale` (1.0) まで**まっすぐ**増える |
| 濃さ | 段の中の進み具合で 0.82 → 1.0 |
| 軌跡 | 段の半ば（`trailFrom` 0.5 = Lv15 / Lv25 …）から出て、段の終わりに向けて濃くなる |
| 光の量 | 段が上がるほど強い（絵を作るときに焼き込む） |

大きさを**段ごとの階段にしない**のは、Lv19 の武器が Lv20 の武器より
大きく見える逆転を防ぐため（テストで固定: §15）。

### 5.5.3 節目の演出

段が変わったとき、その円のところに武器の名前と閃光を出す。
`weapons.milestones`（既定 `[50, 100]`）の段だけ、画面中央にも短いバナーを出す。

```
        LEVEL 50                        LEVEL 100
ENERGY SCYTHE – LIFESTEAL      LEGENDARY CORE – NOVA
        1.6 秒                          2.2 秒
```

段が変わる回はどの段でも、その円のところに武器の名前と閃光が出て、
LIVE EVENT に「武器の名前 + 能力の名前」が 1 行だけ流れる。

**ゲームは止まらない。** バナーが出ている間も敵は湧き、円は動き、
戦闘も得点も進む（検証済み: §13.3）。

### 5.5.4 NPC との見分け

仮の視聴者（§11）の円は、本物と必ず見分けが付くようにする。

| | 視聴者 | NPC |
| --- | --- | --- |
| 円のふち | 実線 | **破線** |
| 武器の色 | 段の色 | 灰色 `#5b6472` |
| 武器の濃さ | そのまま | `npcAlpha` 0.62 倍 |
| 光り足し | あり | なし |

同じレベルなら **NPC のほうが必ず地味に見える**。誰も居ない間も盤面は
動いていてほしいが、その円が本物の視聴者の成果に見えると、
ランキングの意味が薄れるため。

### 5.5.5 プロフィール画像を隠さない

- 武器は円の**外側**（半径の 1.0 倍より外）にだけ描く
- 描く順番は **武器 → 円**。あとから円を描くので、武器が画像の上に来ることはない
- 攻撃エフェクトは円の中心ではなく**敵との接点**に描く
- レベルの数字や武器名などの浮かぶ文字は、円の**上**へ逃がす

実測（円の内側 80% のうち、プロフィール画像がそのまま見えている画素）:

| | Lv1 | Lv50 | Lv100 |
| --- | --- | --- | --- |
| 見えている割合 | 88.1% | 87.8% | 85.8% |

残りはレベルのバッジ（円の中に置いている。§9.2）。

### 5.5.6 描き方と速さ

部品ごとに、**中身が入っている範囲ぴったり**の絵を 1 回だけ作って貼る。
範囲は絵を描いたあとに画素を測って決めるので、手で書いた範囲がずれて
武器の端が切れることがない。

円をまるごと囲む四角で貼る実装も試したが、Lv10 の小さな剣 1 本のために
円の 12 倍の面積を塗ることになり、**それだけで frame rate が半分に落ちた**
（100 人負荷で 58fps → 26fps）。塗るのは中身のあるところだけにする。

| 設定 | 既定値 | 内容 |
| --- | --- | --- |
| `weapons.extent` | 1.8 | 武器が円の外へ出る量（半径の倍率） |
| `weapons.minRadiusPx` | 7 | これより小さく映る円には描かない |
| `weapons.detailRadiusPx` | 16 | 軌跡を出す最小の大きさ |
| `weapons.attackEffects.max` | 40 | 攻撃エフェクトの同時数 |
| `weapons.attackEffects.lifeMs` | 220 | 1 つが消えるまで |

---

## 6. アイテム仕様

一定間隔でフィールドに出現。**視聴者の円だけが拾える**（敵は素通り）。

| id | 表示 | 効果 | 出現比 |
| --- | --- | --- | --- |
| `power` | POWER | 100 コインギフト相当のレベル（＝最大レベル）まで引き上げ、HP 全快、MAX 状態へ突入 | 1 |
| `heal` | HEAL | HP 全快。MAX 状態中なら**残り時間 +8 秒**（残り 30 秒まで） | 1.4 |
| `speed` | SPEED | 速さ ×1.6（その円の元の速さの 2.4 倍まで） | 1 |

| 設定 | 既定値 |
| --- | --- |
| `spawn.intervalMs` | 14,000ms |
| `spawn.firstDelayMs` | 8,000ms |
| `spawn.maxAlive` | 3 |
| `lifetimeMs` | 30,000ms（消滅前に点滅） |
| `radius` | 30 |

### 6.1 不利益を出さない保証

アイテムの効果適用時、**どの値も「現在値と新値の大きいほう」しか採らない**。
レベル・HP・攻撃力・半径・速さのいずれも下がらない。
（例：レベルが上がると通常は速さが上がるが、仮に下がる設定でもアイテムでは下がらない）

テスト `拾って弱くなる値は 1 つも無い` が、全アイテム × Lv1/20/200 の円で検証している。

---

## 7. 物理仕様

### 7.1 移動

| 対象 | 既定 | 内容 |
| --- | --- | --- |
| 敵 | `linear` | 等速直線運動。壁で反射 |
| 視聴者円 | `linear` | 等速直線運動。壁で反射 |

`enemies.movement.mode: 'wander'` で徘徊＋追尾、`viewers.movement.mode: 'seek'` で
最寄りの敵を追尾する動きに戻せる。

### 7.2 衝突

| 設定 | 既定値 | 内容 |
| --- | --- | --- |
| `collision.bounce` | true | ぶつかると反射（ビリヤード式） |
| `collision.restitution` | 1 | 反発係数 |
| `collision.massFromRadius` | true | 質量 = 半径²。大きい円ほど押し勝つ |
| `collision.keepSpeed` | true | 反射後も速さを維持（止まる円を作らない） |
| `collision.enemiesArePushed` | **false** | 視聴者円は敵を押さない（跳ね返るだけ） |
| `collision.separation` | 140 | 重なりを解消する速さ |
| `collision.separationRatio` | 0.3 | 深い重なりを 1 フレームで解消する割合 |

- 押し離す量は質量で分配し、壁に阻まれて動けなかったぶんは相手側へ回す
  （角で「押しても壁に戻される」状態を防ぐ）
- `enemiesArePushed: false` の理由：円が数百個になると四方から押された敵が
  重なって隅に固まるため（100 人負荷で重なり最大 54 を観測）

### 7.3 端に溜まらないための仕掛け

壁の反射は速度の x/y 成分の絶対値を保つため、**壁と平行な向きで生まれた円は
一生その壁沿いを往復する**。以下で対処する。

| 設定 | 既定値 | 内容 |
| --- | --- | --- |
| `motion.aimAtCenter` | 0.6 | 生成時、中央方向 ±72 度の範囲へ撃ち出す |
| `motion.minWallAngleDeg` | 20 | 軸に平行に近い向きを避ける |
| `motion.recenterAfterMs` | 4,000 | 壁際に居続けたら中央へ向け直す |
| `motion.wallBand` | 1.8 | 「壁際」と見なす距離（半径の倍数） |

実測（2 分間、面積比が理想値）:

| | 対策前 | 対策後 | 面積比 |
| --- | --- | --- | --- |
| 四隅にいた割合 | 28.7% | 13.3% | 16% |
| 中央 1/3 にいた割合 | 2.0% | 9.5% | 11% |
| 敵同士の重なり最大 | 13.5 | 0 | 0 |

### 7.4 フィールドの自動拡縮

| 設定 | 既定値 | 内容 |
| --- | --- | --- |
| `field.width` / `height` | 1080 / 1920 | 基準の広さ |
| `expand.growAt` | 0.30 | 占有率がこれを超えたら 1 段階拡大 |
| `expand.shrinkAt` | 0.12 | これを下回ったら 1 段階縮小 |
| `expand.step` | 1.35 | 1 段階の倍率（面積では約 1.8 倍） |
| `expand.maxSteps` | 3 | 最大 3 段階（×2.46） |
| `expand.durationMs` | 2,000 | 変化にかける時間 |
| `expand.cooldownMs` | 4,000 | 変化後、次の判定までの待ち |

- 判定は**占有率**（円と敵の面積の合計 ÷ 床面積）。数ではなく面積で見るのは、
  レベル 100 の円が Lv1 の約 6 倍の面積を持つため
- 変化中は ease-in-out で滑らかに補間し、**中の座標も同じ割合で拡縮**する
  （位置関係は変わらず、半径と速さは据え置き ＝ 密度だけが下がる）
- 画面には `STAGE EXPANDED` / `STAGE SHRINKING` を英語で表示（表示時間 = 変化時間）

### 7.5 フレーム制御

| 設定 | 既定値 | 内容 |
| --- | --- | --- |
| `loop.maxDeltaMs` | 50 | 1 フレームで進める時間の上限 |

タブを裏に回して戻したときに、まとめて何秒も進んで円が壁を突き抜けるのを防ぐ。

---

## 8. 戦闘・得点仕様

### 8.1 戦闘

- 視聴者円と敵が接触したとき、双方が自分の攻撃間隔でダメージを与え合う
- **Lv10 以上の円は、回っている武器でも敵を斬る**（接触していなくても届く。§5.5.1.1）
- 敵の HP が 0 で撃破。円の HP が 0 で消滅
- 敵は「誰がどれだけ削ったか」を常に記録する（`contributions`）

`damage` イベントは `source` を持つ（`'body'` 接触 / `'weapon'` 武器 /
`'nova'` 衝撃波）。画面はこれを見て、武器で斬ったときだけ斬撃を出す。

100 人が同時に操作した状態での内訳（2 分間の実測）:

| | 本体 | 武器 | 衝撃波 |
| --- | --- | --- | --- |
| 当たった回数 | 5,978 | 2,475 | 7 |
| 与ダメージ | 69.7% | 29.5% | 0.8% |

撃破ペースは毎秒 8.1 体（武器を切ると 9.7 体）。撃破の速さは敵の湧く間隔で
頭打ちになるので、武器を足しても盤面が空にはならない。

### 8.2 得点配分

| 設定 | 既定値 | 内容 |
| --- | --- | --- |
| `scoring.mode` | `lastHit` | `lastHit` = とどめを刺した 1 人が全ポイント / `damage` = 与ダメージ比で分配 |
| `scoring.killCredit` | `lastHit` | `damage` モード時、KILL 数を誰に付けるか（`lastHit` / `topDamage`） |
| `scoring.pointsPerDamage` | 0 | ダメージ 1 につき入るスコア |
| `scoring.minShare` | 0.02 | `damage` モードで無視する貢献の下限 |

KILL 数は必ず 1 人にのみ加算する（分割すると整数でなくなるため）。

### 8.2.1 連続撃破（コンボ）

短い間に続けて倒すと、**とどめを刺した人の**ポイントに倍率が掛かる。

| 設定 | 既定値 | 内容 |
| --- | --- | --- |
| `combo.enabled` | true | – |
| `combo.windowMs` | 4,000 | この時間内の次の撃破で伸びる |
| `combo.showFrom` | 3 | 画面に `COMBO x3` と出し始める回数 |
| `combo.bonusPerHit` | 0.1 | 1 段あたりの倍率 |
| `combo.maxBonus` | 1.0 | 倍率の上限（**最大 2 倍**） |

- 数えるのは人ごと。他人の撃破では伸びない
- 上限を 2 倍で止めているのは、コンボ 1 回で順位がひっくり返ると、
  こつこつ育てた人の積み上げが軽くなるため
- 途切れると 1 に戻る

### 8.3 ランキング

ユーザーごとに保持するデータ:

```js
{ userId, userName, profileImageUrl, kills, damage, score,
  level, maxLevel, queued, lastActivity }
```

| 設定 | 既定値 |
| --- | --- |
| `ranking.sortBy` | `score`（`kills` / `damage` も選択可） |
| `ranking.order` | `desc` |
| `ranking.size` | 10 |

- 同点時は score → kills → damage → 先に到達した人の順
- 11 位以下は表示しない
- `maxLevel` は下がらない（新しい円になっても最大到達レベルは残る）
- 実行中に `CB.leaderboard.setSort('kills')` で基準を変更できる

---

## 9. 画面仕様

### 9.1 レイアウト（ゲーム画面 `game.html`）

TikTok LIVE の縦画面（**1080×1920 = 9:16**）を基準にする。

**画面いっぱいがスタジアム**で、そのうえに表示を重ねる作り。盤面を区切って
並べると、狭い端末ほど肝心のスタジアムが小さくなってしまう。

```
┌──────────────────────────┐  ← ネオンの外枠（上下左右 4 本の帯）
│ WAVE 3          ● LIVE   │
│ ENEMIES DEFEATED  2,951  │  ← 上に暗幕。絵に溶けないように
│ ENEMIES 48 CIRCLES 245   │
│ BOSS ▓▓▓▓▓▓░░░░  5,168   │
│ ┌────────────┐           │
│ │ TOP 5      │  ○   ●    │  ← 左に重ねる。盤面が透ける濃さ
│ │ 1 @A   128 │      ○    │
│ │ …          │  ●        │
│ │ @Z #23 …   │           │  ← 圏外の人の順位
│ └────────────┘     ○     │
│        ●      ○          │  ← ここが 1080×1920 のスタジアム
│   ○         ●            │
│ @C LIKE → Lv7            │  ← LIVE EVENT（3 行・自動で消える）
└──────────────────────────┘
```

盤面 (canvas) が画面を丸ごと覆い、ステータス・TOP 5・イベントはその上に浮かぶ。

#### 縦横比

盤面の座標も**画面と同じ比**にする。配信に映す画面 (`primary`) が
自分の canvas の比を `engine.setFieldAspect()` で渡し、engine は幅を保ったまま
高さだけを合わせる。合わせないと上下か左右に隙間ができて、
「画面一面がスタジアム」にならない。

| 画面 | canvas | フィールド |
| --- | --- | --- |
| 1080×1920 | 1080×1920 | 1080×1920 |
| 390×844 | 390×844 | 1080×2337 |

操作画面のプレビューは比を変えない（小さい窓の比に盤面が引きずられると、
配信の見え方と違ってしまうため）。プレビュー側は中央に寄せて余白を出す。

#### 背景

`ui.background.url` に 1080×1920 の画像を敷ける。**画像の座標と盤面の座標が
そのまま一致する**（画像の左上が (0, 0)、右下が (1080, 1920)）。

| 設定 | 既定値 | 内容 |
| --- | --- | --- |
| `ui.background.url` | null | 画像の URL。null なら色と罫線 |
| `ui.background.dim` | 0.28 | 画像に重ねる暗幕。円と敵を見やすくする |
| `ui.background.color` | `#090a18` | 画像が無いときの下地 |

- 操作画面の **BACKGROUND** から選ぶ（`blob:` にするだけで、どこにも送らない）
- OBS のブラウザソースからは `game.html?bg=<URL>&dim=0.3`
- 画像は 1 枚のオフスクリーンに焼いてから貼るので、毎フレームの費用は貼る 1 回だけ
- 読み込みに失敗したら既定の下地に戻る（ゲームは止まらない）

#### ネオンの外枠

上下左右の**4 本の帯**。色は流れる。

画面いっぱいの 1 枚を `mask` でくり抜いて作る実装も試したが、色を流すたびに
画面全体（200 万画素）を塗り直すことになり、**それだけで frame rate が
半分以下になった**（54fps → 22fps）。帯なら塗り直すのは 8 万画素ほどで、
目に見える費用はない。

#### TOP 5

`ranking.size`（既定 5）行。**盤面が透けて見える濃さ**にしてある。
濃くすると読みやすい代わりに、その下で戦っている円が見えなくなる。

| | 値 |
| --- | --- |
| 下地 | `rgba(6, 8, 22, 0.5)` |
| 幅 | `clamp(168px, 48vw, 400px)` |
| 位置 | 画面の左（右端は TikTok の UI と重なるので空ける） |

値は数字だけを出す（何の数字かは見出しに出ているので、行ごとに繰り返すと
そのぶん名前が削られて読めなくなる）。

#### 対応サイズ

320×568 / 375×667 / 390×844 / 412×915 / 430×932 / 1080×1920 で検証済み。
どのサイズでも次を満たす:

- 横スクロール・意図しない縦スクロールなし（1 画面に収まる）
- 盤面が画面いっぱい（canvas = 画面、engine の比も一致）
- TOP 5 が 5 行とも表示され、プロフィール画像も行内に収まる
- 文字が切れない（すべて `clamp()` の最小値つき）
- 横向きでは `PORTRAIT MODE REQUIRED` を出してゲームを表示しない

#### 実装上の決めごと

| 事項 | 内容 |
| --- | --- |
| 高さ | `100dvh`（`100vh` だとスマホのアドレスバーぶん下が切れる） |
| ふち | `env(safe-area-inset-*)` + 右下を少し多めに空ける（TikTok の UI と重なるため） |
| 文字・アイコン | `clamp()` の最小値つき。TOP 5 の中身は行の高さ（`cqh`）から決める |
| 重ねる表示 | 上下に暗幕の帯。全画面の 1 枚にすると、そのぶん毎フレーム重ねる面積が増える |
| 操作 | 見る専用。`touch-action: none` / `user-select: none` |
| リサイズ | 物理は時間で進むので、大きさが変わっても速さは変わらない |

### 9.1.1 レイアウト（操作画面 `index.html`）

配信には映さない。触るものを全部ここに置く。

| 区画 | 内容 |
| --- | --- |
| GAME WINDOW | `[ OPEN GAME WINDOW ]` |
| TIKTOK LIVE | LIVE の URL / @ユーザー名 と CONNECT、接続状態 |
| AUDIO | 効果音の入切・音量、BGM の MP3 選択・再生・音量 |
| BACKGROUND | 盤面に敷く 1080×1920 の画像と、その暗幕の濃さ |
| TEST EVENTS | LIKE / FOLLOW / SHARE / GIFT / JOIN / COMMENT / CROWD / BOSS / SWARM / RESET |
| PREVIEW | 同じ盤面の小さい表示（音は鳴らさない） |
| GAME / ENEMIES / LIKE… | 設定スライダー（触ったその場で `config` に反映） |

設定スライダーは `js/control.js` の `SETTINGS` に 1 行足せば増える。
配信中に変えても壊れない値だけを出している（フィールドの大きさなど、
途中で変えると座標が飛ぶものは出していない）。

### 9.2 円の表示

| 要素 | 内容 |
| --- | --- |
| 中身 | TikTok プロフィール画像（取得できなければ頭文字と色） |
| 外周 | 所有者ごとの色（userId のハッシュから決定） |
| HP | 残量を外周のリングで表示（満タン時は非表示） |
| レベル | 円の内側下部にバッジ（`Lv42`、最大レベルは金色の `MAX`） |
| MAX 状態 | 虹色の回転オーラ + 残り時間の白いリング |
| 名前 | 生まれてから `ui.nameTagMs`（既定 4 秒）だけ円の下に `@ユーザー名` |
| 武器 | レベルに応じた武器（§5.5）。円の**外側**にだけ描く |
| NPC | ふちが破線、武器は灰色（§5.5.4） |

### 9.3 演出（何が起きたかを一目で分かるようにする）

いずれも**盤面を止めない**。止めるとその間ゲームが見られない。

| 出るもの | いつ | どこ | 長さ |
| --- | --- | --- | --- |
| `@ユーザー名` | 円が生まれた | 円の下 | `ui.nameTagMs` 4 秒 |
| `LIKE → Lv7` | 円が生まれた | LIVE EVENT | `ui.eventLifeMs` 9 秒 |
| `Lv12` | レベルが上がった | その場に飛ぶ | `ui.floatMs` 1.1 秒 |
| `+42` | 敵を倒した | その場に飛ぶ | 同上 |
| `+128  COMBO x5` | 連続撃破が `combo.showFrom` 以上 | その場に飛ぶ | 同上 |
| `BOSS DEFEATED` | HP が `ui.bossBarMinHp` 以上の敵を倒した | その場に飛ぶ | 同上 |
| `NEON BLADE` など | 武器の段が変わった | 円の上 + 閃光 | 同上 |
| `LEVEL 50 / NEW WEAPON` | Lv50 に到達 | 中央のバナー | 1.6 秒 |
| `LEGENDARY` + `LEVEL 100` | 最大レベルに届いた | 円の上 + 中央のバナー | 2.2 秒 |
| `NEW RANK #7` / `RANK UP #2` | ランクイン / TOP3 内で上昇 | LIVE EVENT | 9 秒 |
| `@name #23 – 40 TO TOP10` | 直近に動いた人が TOP10 外 | TOP10 の下 | 2 秒ごとに更新 |
| `BOSS ▓▓▓░ 5,168` | 大物が居る間 | ステータス | 居る間ずっと |
| `WAVE 4` | ウェーブが進んだ | 中央のバナー | 1.8 秒 |
| `BOSS APPEARED` など | 特殊イベント | 中央のバナー | 2.2 秒 |
| `STAGE EXPANDED` | フィールドが広がる / 戻る | 中央のバナー | 2 秒 |

やらないこと（意図的に入れていない）:

- 文字を出しっぱなしにする（LIVE EVENT は 3 行・9 秒で消える）
- 画面を覆う通知（バナーは中央の小さい枠に収める）
- ランキングを点滅させる（変化した人を LIVE EVENT に 1 行出すだけ）
- 常時 BOSS（`ui.bossBarMinHp` 以上の敵が居るときだけゲージが出る）
- 演出のためにゲームを止める（すべて描画側だけで完結する）

浮かぶ文字（`renderer.float`）の大きさは**フィールド座標**で指定する。
画面 px で指定すると、スマホでは円より文字が大きくなって盤面が見えなくなる。

### 9.4 URL パラメータ

| パラメータ | ページ | 内容 |
| --- | --- | --- |
| `?offline=1` | 両方 | TikTok に接続しない |
| `?demo=0` | 両方 | デモ視聴者を出さない |
| `?sound=0` / `?mute=1` | `index.html` | 効果音を鳴らさない |
| `?sort=kills` | `index.html` | ランキングの基準 |
| `?game=1` | `index.html` | 開いた直後にゲーム画面も開く |
| `?bridge=<URL>` | 両方 | 中継サーバーを直接指定 |
| `?bg=<URL>` | `game.html` | 盤面に敷く背景画像 |
| `?dim=0.3` | `game.html` | 背景に重ねる暗幕の濃さ |

---

## 10. 音仕様

音源ファイルは持たず、WebAudio で合成する。

| 名前 | いつ | 種類 |
| --- | --- | --- |
| `hit` | 敵に当たった | 打撃音 |
| `kill` | 敵を倒した | 敵の `killPitch` で高さが変わる |
| `lose` | 円が力尽きた | 低い短音 |
| `spawn` | LIKE で円が出た / 育った | 上がり音 |
| `follow` / `share` / `gift` | 各イベント | 2〜3 音 |
| `item` | アイテムを拾った | 3 音の上がり |
| `rank` | 1 位交代 / MAX 到達 / ステージ変化 | ベル |

| 設定 | 既定値 |
| --- | --- |
| `audio.volume` | 0.35 |
| `audio.maxPerSecond` | 22 |
| 各音の `minIntervalMs` | 55〜1,200ms |

ブラウザの自動再生制限により、操作前は鳴らない。その間はバッジを表示し、
最初のクリックで開始する（ゲームの進行は止まらない）。OBS では制限を受けない。

---

## 11. デモ視聴者

TikTok に接続していない / まだ誰も反応していない間だけ、仮の視聴者が動く。

| 設定 | 既定値 |
| --- | --- |
| `demo.enabled` | true |
| `demo.players` | 6 |
| `demo.intervalMs` | 900 |
| `demo.keepCircles` | 18（この数を下回るときだけ補充） |
| `demo.stopOnRealEvent` | true |
| `demo.clearOnRealEvent` | true |
| `demo.retireIntervalMs` | 260（1 つずつ引き上げる間隔） |

- 本物のイベントが 1 件でも届いたら停止。ランキング記録は即座に削除
- 円は**一斉には消さず 1 つずつ**引き上げる（一斉に消すと画面がリセットされたように見えるため）
- 引き上げ中の円が敵を倒しても、デモの名前はランキングに戻らない

---

## 12. 中継サーバー（tikhub）

### 12.1 エンドポイント

| パス | 内容 |
| --- | --- |
| `/` | ゲーム 1 つなら本体、複数なら選択画面 |
| `/<ゲーム名>/` | 各ゲーム（`package.json` の name が URL になる） |
| `/events` | イベント配信（SSE）。ゲームが複数でも 1 本 |
| `/connect` `/disconnect` | 接続先の変更（同一 PC からのみ） |
| `/health` | 状態確認 |

### 12.2 ゲームの探索

- 起動場所から上へ 3 階層たどり、`index.html` と `js/game.js` があるフォルダを**全部**配信
- 複数見つかった場合は起動時に URL を列挙し、`/` で選択画面を出す
- `--game=<フォルダ>` で 1 つに固定できる
- `npm run update` も見つかった全ゲームを更新し、`package.json` の name で
  更新元リポジトリを判定する（取り違え防止）

### 12.3 中継サーバーの探索（ゲーム側）

1. `?bridge=` があればそれだけ
2. ページ自身のオリジン + `/events`
3. `http://127.0.0.1:8787/events` → `http://localhost:8787/events`

最後の候補は接続失敗後も開いたままにするため、ゲームを先に開いて
tikhub を後から起動しても自動で繋がる。

---

## 13. 性能

### 13.0 ウィンドウを 2 つ開いたときの実測（100 人が同時に操作）

| | 操作画面（1200×900） | ゲーム画面（540×960） |
| --- | --- | --- |
| FPS | 40.9 | 40.9 |
| 最悪フレーム | 50ms | 48ms |

20 秒間で players=100 / circles=106 / enemies=32 / 撃破 332、
イベント取りこぼし 0（受信 1,336 / 反映 1,336）、エラー 0。
ゲーム画面 1 つだけ（1080×1920）なら 54fps。

画面いっぱいのスタジアムにしたぶん、毎フレーム塗る面積が
おおよそ 2 倍になっている（正方形の盤面を画面の一部に置いていたときは 60fps）。

`app.update()` が「直前に進めたばかりなら何もしない」ので、
画面が 2 つでも物理計算は 1 回ぶんしか走らない。

### 13.0.2 画面まわりの費用（100 人が同時に操作、1080×1920 1 枚）

| | FPS |
| --- | --- |
| 今の実装 | 53.9 |
| ネオンの枠を全画面 1 枚 + mask にする | 22.0 |
| 上下の暗幕を全画面 1 枚にする | −4 |
| ランキングに `backdrop-filter` を掛ける | ほぼ変わらず（外した） |

飾りは「動くもの」と「重なる面積」で決まる。動く飾りは**必要な帯だけ**にする。

### 13.0.1 武器の描画コスト（100 人が同時に操作、画面 2 つ）

| | 武器なし | 武器あり（四角で貼る） | 武器あり（今の実装） |
| --- | --- | --- | --- |
| FPS | 58 | 26 | **54** |

中身のあるところだけを塗るようにして、コストを 6% 程度に収めた（§5.5.6）。
画面を 2 つ開いた状態では 60fps → 49fps。

### 13.3 演出中もゲームは止まらない

Lv50 / Lv100 のバナーが出ている 1.6〜2.2 秒の間も、敵の生成・移動・
戦闘・得点はそのまま進む（バナーは DOM の上に重なるだけで、
ループには一切触らない）。検証では演出中に敵が 2 体湧いた。

### 13.1 実測（100 人が同時に操作、中継サーバー経由、1080×1920）

| | 実配信に近い負荷 | 全員が全力で連打（実際の 10〜40 倍） |
| --- | --- | --- |
| 撃破ペース | 毎秒 14 体 | 毎秒 17 体 |
| 円 | 43 個 | 40〜60 個 |
| 敵 | 17 体 | 46〜48 体 |
| FPS | 60 | 60 |
| イベント取りこぼし | 0 | 0（9,467 件すべて反映） |
| メモリ | 安定 | 3〜9MB で安定 |

### 13.2 主な最適化

| 箇所 | 内容 | 効果 |
| --- | --- | --- |
| アイコン | 読み込み時に 1 回だけ丸く切り抜く | 描画 2.9ms → 1.1ms |
| レベル表示 | レベルごとに 1 枚だけ描いて貼り回す | 0.9ms → 0.33ms |
| 当たり判定 | フィールドを格子に切り、隣の升だけ見る | 400 個で 1 フレーム 0.1ms |
| 当たり判定 | 升を毎フレーム作り直さず使い回す | 最悪フレーム 42ms → 3.8ms |
| ランキング | 1 位判定を毎フレーム 1 回に | 535ms → 7ms |
| 背景 | 1 枚作って貼るだけ | 毎フレームの塗り直しを削減 |

エンジンの 1 フレームは中央値 0.55ms / p99 1.2ms、18,000 フレーム中 16.7ms 超えは 0 回。

---

## 14. 拡張ポイント

| やりたいこと | 方法 |
| --- | --- |
| 敵の種類を足す | `enemies.types` に 1 行 / `engine.addEnemyType()` |
| アイテムを足す | `items.types` に 1 行 / `engine.addItemType()` |
| イベントボス | `engine.addEnemyType()` + `engine.spawnEnemy(id)` |
| ランキング基準の変更 | `ranking.sortBy` / `leaderboard.setSort()` |
| 得点をダメージ配分に | `scoring.mode: 'damage'` |
| コメントでの操作 | `session.on('comment')` に処理を足す（イベントは既に届いている） |
| 特殊イベントを足す | `director.events` に 1 行 |
| 武器の段を足す・変える | `weapons.tiers` に 1 行 + `js/weapons.js` の `PAINTERS` に描き方 |
| 特殊能力を足す | `weapons.tiers[].ability` の 5 部品を組み替える |
| 武器の強さを変える | `weapons.combat.damage` と段ごとの `hit.damage` |
| 操作画面に設定を出す | `js/control.js` の `SETTINGS` に 1 行 |
| 演出を足す | `js/game-view.js` でイベントを購読して `renderer.float()` / `pushEvent()` |
| デスクトップアプリ化 | 操作画面 = Control Window / `game.html` = Game Window の 2 枚をそのまま使う |
| 複数 LIVE 接続 | LIVE ごとに engine + session を作り `router.attach(liveId, session)` |
| 別の配信サービス | アダプタとルーターをもう 1 組書く（下流は無変更） |

---

## 15. テスト

`npm test`（Node の標準テストランナー。ブラウザ不要）で **189 件**。

| ファイル | 内容 |
| --- | --- |
| `event-router.test.js` | イベント翻訳・ギフト換算・ユーザー正規化・JOIN |
| `game.test.js` | 敵の生成・移動・反射・質量・レベル曲線・MAX・HEAL・分布 |
| `game-session.test.js` | レベルの上がり方・MAX 後の再スタート・入室・キュー・得点配分 |
| `leaderboard.test.js` | 並べ替え・同点処理・TOP10 の打ち切り |
| `items.test.js` | 出現・回収・敵は拾えないこと・不利益が出ないこと |
| `stage.test.js` | フィールドの拡縮・2 秒かけること・往復防止 |
| `idle-loop.test.js` | イベント 0 件でもゲームが回ること・デモの引き上げ |
| `load.test.js` | 100 人負荷でのフレーム時間・上限・ランキング |
| `audio.test.js` | 効果音の間引き |
| `tiktok-adapter.test.js` | 中継サーバーの探索順 |
| `director.test.js` | ウェーブ進行・特殊イベント・**円とランキングを消さないこと** |
| `weapons.test.js` | 段の割り当て・中間レベルの伸び・円の基本値を変えないこと |
| `weapon-combat.test.js` | 当たり判定（届く距離・角度・腕の向き）・特殊能力 5 種・**見た目とルールで段が一致すること** |

`npm run check` で全ファイルの構文チェック。

---

## 16. 用語

| 用語 | 意味 |
| --- | --- |
| 円 / circle | 視聴者が持つ実体。レベルを持ち、敵を攻撃する |
| 敵 / enemy | ゲームが自動生成する実体。撃破されるとポイントになる |
| MAX 状態 | レベル 100 到達後の 20 秒間。虹色オーラで大幅強化される |
| 燃え尽き / burnout | MAX 状態の時間切れによる消滅 |
| 順番待ち / queue | 保有上限を超えたぶんが待機する列 |
| 占有率 / coverage | 円と敵の面積の合計 ÷ 床面積。フィールド拡縮の判定に使う |
| ウェーブ / wave | 一定時間ごとに進む区切り。進んでも円とランキングは消えない |
| 段 / tier | 武器の段階。Lv1/10/20…100 の 11 段。形・当たり判定・能力が変わる |
| 腕 / arm | 武器の当たり判定 1 本ぶん。等間隔に `arms` 本並ぶ |
| 衝撃波 / NOVA | Lv100 だけが持つ、射程内の敵全部への攻撃 |
| 操作画面 / Control Window | `index.html`。設定と接続。配信には映さない |
| ゲーム画面 / Game Window | `game.html`。9:16。配信に映す |
| ステージ | フィールドの拡大段階（0 〜 3） |
